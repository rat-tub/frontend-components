export default interface PersonCardType {
  image: string; //link to image
  imageAlt: string;
  name: string;
  role: string;
  quote: string;
  github?: string;
  linkedin?: string;
  instagram?: string;
}
